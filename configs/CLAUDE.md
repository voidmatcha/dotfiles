# Claude-specific routing

- Use the configured Opus model at medium effort by default.
- Fable is opt-in for the hardest, longest-running tasks; never switch to it
  automatically for bounded implementation or review work.
- Child model and effort stay with the installed Claude agent metadata.

@~/.agent/AGENTS.md
