# macOS junk
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# Editors / IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.cursorignore.local

# direnv (keep .envrc tracked — projects often commit it for shared dev env)
.direnv/

# Local env files (per-repo .gitignore should still cover these too)
.env
.env.local
.env.*.local

# Claude Code / agent scratch
.claude/settings.local.json

# codegraph per-project knowledge graph (SQLite, large; rebuilds from source)
.codegraph/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
